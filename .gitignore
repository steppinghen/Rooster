# ---- Dependencies ----
node_modules/
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# ---- Environment / secrets ----
# The Supabase anon key is publishable and may live in client code.
# Everything below is for service keys, tokens, and other secrets — never commit.
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
secrets.json
service-account*.json

# ---- Build output ----
dist/
build/
out/
.output/
.vercel/
.next/
.nuxt/
.svelte-kit/
.astro/
.cache/
.parcel-cache/
.turbo/
*.tsbuildinfo

# ---- Netlify ----
.netlify/
# generated by `netlify dev` (edge functions runtime) — not ours to track
deno.lock

# ---- Supabase ----
.branches/
.temp/
supabase/.temp/

# ---- Logs ----
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ---- Testing / coverage ----
coverage/
.nyc_output/
playwright-report/
test-results/
blob-report/
.playwright/

# ---- Editors / tooling ----
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*.bak
*.sb-*
.eslintcache
.stylelintcache

# ---- OS ----
.DS_Store
.AppleDouble
.LSOverride
._*
Thumbs.db
Desktop.ini

# ---- Real business data (PII) ----
# Source workbooks and transformed exports of the contractor book of
# business. Kept local: they are not needed to build or run any site,
# and they must never sit in a publish directory (Netlify uploads the
# whole folder) or in a repo whose visibility might change.
_db/**/*.xlsx
_db/**/*_transformed.csv
