{
  "name": "rooster",
  "version": "0.0.0",
  "private": true,
  "description": "Personal projects monorepo — one repo, several independent Netlify sites",
  "scripts": {
    "dev:portfolio": "cd portfolio && netlify dev",
    "dev:dad-contracting": "cd dad-contracting && netlify dev",
    "dev:hvac-visits": "cd hvac-visits && netlify dev",
    "dev:lab": "cd lab && netlify dev",
    "sync:auth": "for s in hvac-visits lab; do cp _shared/auth-overlay.js _shared/auth-overlay.css \"$s/vendor/\"; echo \"synced $s\"; done",
    "check:noindex": "for p in 8888 8889 8890 8891; do printf '%s ' $p; curl -sI \"http://localhost:$p\" | grep -i x-robots-tag || echo 'NOT RUNNING / NO HEADER'; done"
  },
  "engines": {
    "node": ">=18"
  }
}
